---
description: Acknowledge the {{hook-name}} reminder — resets the timer.
---

Invoke Bash with exactly this command (no `cd`, no chaining, no redirection): `node __HOOK_PATH__ --ack`

The CLI is silent on success. Reply with one short warm line and move on.
