---
description: Show the current {{hook-name}} state — last event, staleness, snooze.
---

The {{Hook-Name}} UserPromptSubmit hook has already produced the status (see the `<{{hook-name}}-action-result>` block in your context). Relay it in one plain-language sentence. Do not invoke any tools.
